#!/bin/bash
# Eski lokal python sunucu — KULLANMA.
# Admin Docker'da: api/ → docker compose up --build -d admin
# Link: http://127.0.0.1:3010
echo "⚠️  Admin artık Docker'da (isgatlasi-admin)."
echo "   cd api && docker compose up --build -d admin"
echo "   Aç: http://127.0.0.1:3010"
exit 1
